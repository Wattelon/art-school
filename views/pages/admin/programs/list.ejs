<%- include(partials + '/header') %>

<h1>Программы обучения</h1>

<a href="/admin/programs/add" class="btn">Добавить программу</a>

<table border="1" cellpadding="8" cellspacing="0">
    <thead>
    <tr>
        <th>Название</th>
        <th>Возраст</th>
        <th>Действия</th>
    </tr>
    </thead>
    <tbody>
    <% programs.forEach(p => { %>
        <tr>
            <td><%= p.title %></td>
            <td><%= p.age_group || '—' %></td>
            <td>
                <a href="/admin/programs/edit/<%= p.id %>">✏️ Редактировать</a>
                <form action="/admin/programs/delete/<%= p.id %>" method="POST" style="display:inline;">
                    <button type="submit" onclick="return confirm('Удалить программу?')">🗑️ Удалить</button>
                </form>
            </td>
        </tr>
    <% }) %>
    </tbody>
</table>

<a href="/admin">← Назад в админку</a>

<%- include(partials + '/footer') %>
