<%- include(partials + '/header') %>

<main class="admin-list">
    <h1 class="admin-list__title">Контакты</h1>

    <div class="admin-list__actions">
        <a href="/admin/contacts/add" class="cta-button">➕ Добавить контакт</a>
    </div>

    <% if (contacts.length === 0) { %>
        <p class="admin-list__empty">Контактов пока нет.</p>
    <% } else { %>
        <table class="admin-list__table">
            <thead>
            <tr>
                <th>Название</th>
                <th>Телефон</th>
                <th>Email</th>
                <th>Соцсети</th>
                <th>Адрес</th>
                <th>Действия</th>
            </tr>
            </thead>
            <tbody>
            <% contacts.forEach(c => { %>
                <tr>
                    <td><%= c.title %></td>
                    <td><%= c.phone || '—' %></td>
                    <td><%= c.email || '—' %></td>
                    <td><%= c.social || '—' %></td>
                    <td><%= c.address || '—' %></td>
                    <td>
                        <a href="/admin/contacts/edit/<%= c.id %>">✏️</a>
                        <form action="/admin/contacts/delete/<%= c.id %>" method="POST" class="admin-list__delete-form">
                            <button type="submit" onclick="return confirm('Удалить контакт?')">🗑️</button>
                        </form>
                    </td>
                </tr>
            <% }) %>
            </tbody>
        </table>
    <% } %>

    <p class="admin-list__back">
        <a href="/admin">← Назад в админку</a>
    </p>
</main>

<%- include(partials + '/footer') %>
