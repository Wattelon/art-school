<%- include(partials + '/header') %>

<main class="admin-list">
    <h1 class="admin-list__title">Педагоги</h1>

    <div class="admin-list__actions">
        <a href="/admin/teachers/add" class="cta-button">➕ Добавить преподавателя</a>
    </div>

    <% if (teachers.length === 0) { %>
        <p class="admin-list__empty">Преподавателей пока нет.</p>
    <% } else { %>
        <table class="admin-list__table">
            <thead>
            <tr>
                <th>Имя</th>
                <th>Биография</th>
                <th>Действия</th>
            </tr>
            </thead>
            <tbody>
            <% teachers.forEach(t => { %>
                <tr>
                    <td><%= t.name %></td>
                    <td><%= t.bio %></td>
                    <td>
                        <a href="/admin/teachers/edit/<%= t.id %>">✏️</a>
                        <form action="/admin/teachers/delete/<%= t.id %>" method="POST" class="admin-list__delete-form">
                            <button type="submit" onclick="return confirm('Удалить преподавателя?')">🗑️</button>
                        </form>
                    </td>
                </tr>
            <% }) %>
            </tbody>
        </table>
    <% } %>

    <p class="admin-list__back">
        <a href="/admin">← Назад в админку</a>
    </p>
</main>

<%- include(partials + '/footer') %>
